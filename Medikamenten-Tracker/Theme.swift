import SwiftUI

enum AppTheme {
    // Hauptfarben - Dunkles Marineblau
    static let background = Color(red: 0.06, green: 0.09, blue: 0.16)
    static let cardBackground = Color(red: 0.10, green: 0.14, blue: 0.24)
    static let accent = Color(red: 0.30, green: 0.50, blue: 0.85)
    static let accentLight = Color(red: 0.45, green: 0.65, blue: 1.0)
    static let textPrimary = Color.white
    static let textSecondary = Color(red: 0.60, green: 0.65, blue: 0.75)
    static let warning = Color.orange
    static let danger = Color.red
    static let success = Color(red: 0.30, green: 0.80, blue: 0.50)
    static let tabBarBackground = Color(red: 0.08, green: 0.11, blue: 0.20)

    // Kalender
    static let calendarDotGreen = Color(red: 0.30, green: 0.80, blue: 0.50)
    static let calendarDotOrange = Color.orange
    static let calendarDotRed = Color.red
}
